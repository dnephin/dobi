
machine:
  services:
    - docker

general:
  branches:
    ignore: [gh-pages]

dependencies:
  override: [./script/ci-deps]
  cache_directories: [vendor, ~/docker]

test:
  override: [./script/ci-run]

deployment:
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    commands: ['./circle/dobi release']
