
machine:
  services:
    - docker
  environment:
    DOCKER_API_VERSION: '1.20'

dependencies:
  override:
    - ./script/ci-deps
  cache_directories:
    - vendor
    - ~/docker

test:
  override:
    - ./dist/bin/dobi
